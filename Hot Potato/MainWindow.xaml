<Window x:Class="HotPotatoLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hot Potato Command Center" Height="850" Width="1200" 
        Background="#121212" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="TabBtnStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="#FFD700" BorderThickness="0,0,0,0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="25,0,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#252526"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="bd" Property="BorderThickness" Value="4,0,0,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{DynamicResource ComboBoxToggleButton}" Grid.Column="2" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" Template="{DynamicResource ComboBoxTextBox}" 
                                     HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,3,23,3" Focusable="True" 
                                     Background="Transparent" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#252526" BorderThickness="1" BorderBrush="#444"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="2" Background="#252526" BorderBrush="#444" BorderThickness="1" />
                <Path x:Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" Padding="5">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#444"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#FFD700"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,35,0,35">
                    <TextBlock x:Name="LblTitle" Text="🥔 Hot Potato" FontSize="28" FontWeight="Bold" Foreground="#FFD700" HorizontalAlignment="Center" MouseDown="LblTitle_MouseDown" Cursor="Hand" ToolTip="Click 5x to unlock Omar Mode (Admin Settings)"/>
                    <TextBlock FontSize="10" Foreground="#555" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <RadioButton x:Name="TabHomeBtn" Content="🏠   DASHBOARD" Style="{StaticResource TabBtnStyle}" IsChecked="True" Checked="Tab_Checked" Tag="TabHome" ToolTip="Start, Stop, and Monitor your server."/>
                    <RadioButton Content="🛡️   GAMEPLAY &amp; SAFETY" Style="{StaticResource TabBtnStyle}" Checked="Tab_Checked" Tag="TabGameplay" ToolTip="Edit Whitelist, Gamerules, and Tree Saver."/>
                    <RadioButton Content="🌍   WORLD &amp; PLUGINS" Style="{StaticResource TabBtnStyle}" Checked="Tab_Checked" Tag="TabWorld" ToolTip="Manage Worlds, Plugins, and Cloud Sync."/>
                    <RadioButton x:Name="TabSystemBtn" Content="🔒   SYSTEM CONFIG" Style="{StaticResource TabBtnStyle}" Checked="Tab_Checked" Tag="TabSystem" Click="TabSystem_Click" ToolTip="RAM and Java Settings (Locked)."/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="20">
                    <TextBlock Text="ACTIVE PROFILE" Foreground="#444" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="ComboProfiles" Height="35" SelectionChanged="ComboProfiles_SelectionChanged" DisplayMemberPath="ProfileName" ToolTip="Switch between different server setups."/>
                    <Grid Margin="0,10,0,0">
                        <Button Content="New" HorizontalAlignment="Left" Width="100" Height="30" Click="BtnAddProfile_Click" Background="#333" Foreground="White" BorderThickness="0" ToolTip="Create a fresh server profile."/>
                        <Button Content="Delete" HorizontalAlignment="Right" Width="100" Height="30" Click="BtnDelProfile_Click" Background="#333" Foreground="#FF5252" BorderThickness="0" ToolTip="Permanently delete the current profile."/>
                    </Grid>

                    <Button x:Name="BtnUnlock" Content="🔓 FORCE UNLOCK" Visibility="Collapsed" Height="25" Margin="0,15,0,0" 
                            Background="#FF9800" Foreground="Black" FontWeight="Bold" FontSize="10" Click="BtnUnlock_Click" 
                            ToolTip="Emergency Use Only: Removes the lock file if the server crashed."/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="#121212">

            <Grid x:Name="TabHome" Visibility="Visible" Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="BtnHost" Content="START SERVER" Width="150" Height="50" Background="#2E7D32" Foreground="White" FontWeight="Bold" Click="BtnHost_Click" Margin="0,0,15,0" ToolTip="Launch the server."/>
                        <Button x:Name="BtnStop" Content="STOP" Width="100" Height="50" Background="#C62828" Foreground="White" FontWeight="Bold" Click="BtnStop_Click" IsEnabled="False" ToolTip="Gracefully stop the server."/>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                            <TextBlock x:Name="TxtZtIp" Text="IP: Checking..." Foreground="#AAA" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                            <Button x:Name="BtnCopyIp" Content="📄" Width="30" Height="30" Background="#333" Foreground="White" BorderThickness="0" Click="BtnCopyIp_Click" ToolTip="Copy IP to Clipboard"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock x:Name="TxtUptime" Text="⏱️ 00:00:00" Foreground="#FFD700" FontSize="20" VerticalAlignment="Center" Margin="0,0,25,0" FontWeight="SemiBold" ToolTip="Time since server started."/>
                        <Border x:Name="StatusIndicator" Width="14" Height="14" CornerRadius="10" Background="Red" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="LblStatus" Text="OFFLINE" Foreground="White" FontWeight="Bold" FontSize="20" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Background="Black" CornerRadius="5" BorderBrush="#333" BorderThickness="1">
                    <Grid>
                        <TextBox x:Name="LogBox" Background="Transparent" Foreground="#00E676" FontFamily="Consolas" FontSize="14" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Padding="15" BorderThickness="0" ToolTip="Server Console Output"/>
                        <ProgressBar x:Name="MainProgressBar" VerticalAlignment="Bottom" Height="4" Background="Transparent" Foreground="#FFD700" BorderThickness="0" ToolTip="Cloud Sync Progress"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtCommand" Height="50" Background="#1E1E1E" Foreground="White" BorderThickness="1" BorderBrush="#333" Padding="15,14,0,0" FontSize="15" KeyDown="TxtCommand_KeyDown" ToolTip="Enter server commands here (e.g., /op user)"/>
                    <Button x:Name="BtnSend" Grid.Column="1" Content="SEND" Width="100" Background="#333" Foreground="White" FontWeight="Bold" Click="BtnSend_Click" Margin="10,0,0,0" ToolTip="Execute Command"/>
                </Grid>
            </Grid>

            <Grid x:Name="TabGameplay" Visibility="Collapsed" Margin="40">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Safety Protocol" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <Border Background="#1E1E1E" CornerRadius="8" Padding="20" Margin="0,0,0,30" BorderBrush="#FF5252" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="🛡️ TREE SAVER PANIC BUTTON" Foreground="#FF5252" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="Instantly disables Fire, TNT, and Mob Griefing." Foreground="#888" Margin="0,0,0,15"/>
                                <Button x:Name="BtnTreeSaver" Content="ACTIVATE PROTECTION" Height="50" Background="#C62828" Foreground="White" FontSize="16" FontWeight="Bold" Click="BtnTreeSaver_Click" ToolTip="Click to instantly make the server safe from destruction."/>
                                <TextBlock x:Name="TxtTreeSaverStatus" Text="STATUS: UNPROTECTED" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#555"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Game Rules" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <UniformGrid Columns="2" Margin="0,0,0,20">
                            <CheckBox x:Name="ChkOnline" Content="Online Mode (Premium)" Foreground="White" Click="ChkOnline_Click" ToolTip="Checked: Only paid accounts. Unchecked: Allows cracked accounts (Insecure)."/>
                            <CheckBox x:Name="ChkWhitelist" Content="Enforce Whitelist" Foreground="White" Click="ChkWhitelist_Click" ToolTip="Only allow players listed below to join."/>
                            <CheckBox x:Name="ChkConsole" Content="Show External Console" Foreground="White" Click="ChkConsole_Click" ToolTip="Opens the native Java console window."/>
                        </UniformGrid>

                        <TextBlock Text="Gameplay Limits" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,10,0,15"/>
                        <Border Background="#252526" CornerRadius="8" Padding="15">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="Max Players:" Foreground="#888"/>
                                    <TextBlock Text="{Binding ElementName=SldMaxPlayers, Path=Value, StringFormat={}{0:0} Players}" Foreground="White" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="SldMaxPlayers" Minimum="1" Maximum="100" Margin="0,0,0,15" ValueChanged="Slider_ValueChanged" TickFrequency="1" IsSnapToTickEnabled="True" ToolTip="Maximum number of players allowed at once."/>

                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="Render Distance:" Foreground="#888"/>
                                    <TextBlock Text="{Binding ElementName=SldViewDist, Path=Value, StringFormat={}{0:0} Chunks}" Foreground="White" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="SldViewDist" Minimum="2" Maximum="32" Margin="0,0,0,15" ValueChanged="Slider_ValueChanged" TickFrequency="1" IsSnapToTickEnabled="True" ToolTip="How far players can see. Higher values reduce performance."/>

                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="Simulation Distance:" Foreground="#888"/>
                                    <TextBlock Text="{Binding ElementName=SldSimDist, Path=Value, StringFormat={}{0:0} Chunks}" Foreground="White" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="SldSimDist" Minimum="2" Maximum="32" ValueChanged="Slider_ValueChanged" TickFrequency="1" IsSnapToTickEnabled="True" ToolTip="How far away game mechanics (crops, mobs) work."/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Whitelist" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,25,0,10"/>
                        <TextBox x:Name="TxtWhitelist" Height="80" AcceptsReturn="True" Background="#252526" Foreground="White" BorderThickness="0" Padding="10" TextChanged="TxtWhitelist_TextChanged" ToolTip="Enter one username per line."/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid x:Name="TabWorld" Visibility="Collapsed" Margin="40">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="World Setup" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <Border Background="#1E1E1E" CornerRadius="8" Padding="20" Margin="0,0,0,25">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <RadioButton x:Name="RadioNewWorld" Content="Generate New World" Foreground="White" GroupName="WM" Margin="0,0,20,0" Checked="WorldMode_Changed" ToolTip="Create a fresh world using settings below."/>
                                    <RadioButton x:Name="RadioImportWorld" Content="Import Existing World" Foreground="White" GroupName="WM" Checked="WorldMode_Changed" ToolTip="Use a world folder you paste into the server directory."/>
                                </StackPanel>

                                <StackPanel x:Name="PanelNewWorldOpts">
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Margin="0,0,10,0">
                                            <TextBlock Text="Seed (Optional)" Foreground="#888" FontSize="11"/>
                                            <TextBox x:Name="TxtSeed" Height="30" Background="#252526" Foreground="White" BorderThickness="1" BorderBrush="#444" Padding="5" TextChanged="TxtSeed_TextChanged" ToolTip="Leave empty for random seed."/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                            <TextBlock Text="Game Mode" Foreground="#888" FontSize="11"/>
                                            <ComboBox x:Name="ComboGameMode" Height="30" SelectionChanged="ComboGameMode_SelectionChanged" ToolTip="Default game mode for new players.">
                                                <ComboBoxItem Content="Survival" Tag="survival"/>
                                                <ComboBoxItem Content="Creative" Tag="creative"/>
                                                <ComboBoxItem Content="Adventure" Tag="adventure"/>
                                                <ComboBoxItem Content="Spectator" Tag="spectator"/>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Difficulty" Foreground="#888" FontSize="11"/>
                                            <ComboBox x:Name="ComboDifficulty" Height="30" SelectionChanged="ComboDifficulty_SelectionChanged" ToolTip="World Difficulty.">
                                                <ComboBoxItem Content="Peaceful" Tag="peaceful"/>
                                                <ComboBoxItem Content="Easy" Tag="easy"/>
                                                <ComboBoxItem Content="Normal" Tag="normal"/>
                                                <ComboBoxItem Content="Hard" Tag="hard"/>
                                            </ComboBox>
                                        </StackPanel>
                                    </Grid>

                                    <CheckBox x:Name="ChkHardcore" Content="Hardcore Mode (One Life)" Foreground="#FF5252" Click="ChkHardcore_Click" Margin="0,0,0,15" ToolTip="Players are banned upon death."/>
                                </StackPanel>

                                <StackPanel x:Name="PanelImportWorldOpts" Visibility="Collapsed" Margin="0,10,0,10">
                                    <TextBlock Text="ℹ️ INSTRUCTIONS:" Foreground="#FFD700" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="1. Click the button below to open your profile folder." Foreground="#AAA" Margin="0,0,0,2"/>
                                    <TextBlock Text="2. Paste your world folder there and RENAME it to 'world'." Foreground="#AAA" Margin="0,0,0,15"/>

                                    <Button Content="📂 Open Folder to Paste World" Height="40" Background="#333" Foreground="White" Click="BtnPasteWorld_Click" ToolTip="Opens the server folder."/>
                                </StackPanel>

                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="🗑️ DELETE WORLD (Reset)" Height="40" Background="#441111" Foreground="#FF5252" Click="BtnDelWorld_Click" Margin="0,0,5,0" ToolTip="DANGER: Deletes the current world folder."/>
                                    <Button x:Name="BtnFetchProfiles" Grid.Column="1" Content="☁️ Sync Cloud Profiles" Height="40" Background="#333" Foreground="White" Click="BtnFetchProfiles_Click" Margin="5,0,0,0" ToolTip="Download profiles from your cloud vault."/>
                                </Grid>
                                <CheckBox x:Name="ChkCloud" Content="Enable Automated Cloud Backups" Foreground="White" Margin="0,15,0,0" Click="ChkCloud_Click" ToolTip="Automatically upload world data to cloud on stop."/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Plugin Manager" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <Border Background="#252526" CornerRadius="8" Padding="20" Margin="0,0,0,25">
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="📂 Server Folder" Grid.Column="0" Height="30" Margin="0,0,5,0" Background="#333" Foreground="White" Click="BtnOpenFolder_Click" ToolTip="Open the main server directory."/>
                                    <Button Content="🧩 Open Plugins" Grid.Column="1" Height="30" Margin="0,0,5,0" Background="#333" Foreground="White" Click="BtnOpenPlugins_Click" ToolTip="Open the 'plugins' folder to drop .jar files."/>
                                    <Button Content="🛠️ Mods Folder" Grid.Column="2" Height="30" Background="#333" Foreground="White" Click="BtnOpenMods_Click" ToolTip="Open the 'mods' folder."/>
                                </Grid>

                                <TextBlock Text="Essential Plugins:" Foreground="#888" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <Button Content="EssentialsX" Width="90" Height="30" Margin="0,0,10,0" Click="BtnGetEssentials_Click" Background="#333" Foreground="White" ToolTip="Get EssentialsX (Chat, Spawn, Kits)"/>
                                    <Button Content="WorldEdit" Width="90" Height="30" Margin="0,0,10,0" Click="BtnGetWorldEdit_Click" Background="#333" Foreground="White" ToolTip="Get WorldEdit (Building Tools)"/>
                                    <Button Content="GeyserMC" Width="90" Height="30" Click="BtnGetGeyser_Click" Background="#333" Foreground="White" ToolTip="Allow Bedrock players to join Java servers."/>
                                </StackPanel>

                                <TextBlock Text="Repositories:" Foreground="#888" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="SpigotMC" Width="90" Height="30" Margin="0,0,10,0" Click="BtnRepoSpigot_Click" Background="#444" Foreground="White" ToolTip="Browse Spigot Plugins"/>
                                    <Button Content="Hangar" Width="90" Height="30" Margin="0,0,10,0" Click="BtnRepoHangar_Click" Background="#444" Foreground="White" ToolTip="Browse Paper/Hangar Plugins"/>
                                    <Button Content="CurseForge" Width="90" Height="30" Click="BtnRepoCurse_Click" Background="#444" Foreground="White" ToolTip="Browse CurseForge Plugins"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid x:Name="TabSystem" Visibility="Collapsed" Margin="40">
                <StackPanel>
                    <TextBlock Text="System Configuration" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                    <Border Background="#252526" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Memory Allocation" Foreground="#FFD700" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Slider x:Name="RamSlider" Minimum="2" Maximum="32" Value="4" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,5" ValueChanged="RamSlider_ValueChanged" ToolTip="Drag to set max RAM. Don't set higher than your PC has!"/>
                            <TextBlock Text="{Binding ElementName=RamSlider, Path=Value, StringFormat={}{0:0} GB}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Version Manager" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,10,0,15"/>
                    <Border Background="#1E1E1E" CornerRadius="8" Padding="20" BorderBrush="#333" BorderThickness="1">
                        <StackPanel>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="Loader" Foreground="#888" FontSize="11"/>
                                    <ComboBox x:Name="ComboType" Height="30" SelectionChanged="ComboType_SelectionChanged" ToolTip="Select server type (Paper is recommended for plugins).">
                                        <ComboBoxItem Content="Vanilla / Paper"/>
                                        <ComboBoxItem Content="Forge Modded"/>
                                        <ComboBoxItem Content="Fabric Modded"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                    <TextBlock Text="Java Version" Foreground="#888" FontSize="11"/>
                                    <ComboBox x:Name="ComboJava" Height="30" SelectionChanged="ComboJava_SelectionChanged" ToolTip="Java 17 for 1.18+. Java 8 for 1.16.5 and older.">
                                        <ComboBoxItem Content="Java 17 (Modern)" Tag="java17"/>
                                        <ComboBoxItem Content="Java 8 (Legacy)" Tag="java8"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Jar File" Foreground="#888" FontSize="11"/>
                                    <ComboBox x:Name="ComboJars" Height="30" SelectionChanged="ComboJars_SelectionChanged" ToolTip="Select the specific server jar file to run."/>
                                </StackPanel>
                            </Grid>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="BtnGetPaper" Content="Get Paper" Width="90" Height="30" Margin="0,0,10,0" Background="#333" Foreground="White" Click="BtnGetPaper_Click" ToolTip="Download Paper (Plugins)"/>
                                <Button x:Name="BtnGetForge" Content="Get Forge" Width="90" Height="30" Margin="0,0,10,0" Background="#333" Foreground="White" Click="BtnGetForge_Click" ToolTip="Download Forge (Mods)"/>
                                <Button x:Name="BtnGetFabric" Content="Get Fabric" Width="90" Height="30" Background="#333" Foreground="White" Click="BtnGetFabric_Click" ToolTip="Download Fabric (Lightweight Mods)"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Network Mode" Foreground="#888" Margin="0,20,0,5"/>
                    <ComboBox x:Name="ComboNet" Height="35" SelectionChanged="ComboNet_SelectionChanged" ToolTip="Automatic tries UPnP first. Playit forces a tunnel."/>

                    <TextBlock x:Name="TxtResourceUsage" Text="System Usage: 0%" Foreground="#444" Margin="0,30,0,0" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Grid>
</Window>